server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jobflow
    username: jobflow
    password: jobflow
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

app:
  jwt:
    secret: "lZANVaiDZmaCPeQQBuNomKll43rCrqZZ"
    issuer: "jobflow"
    ttlMinutes: 60
  dev:
    seedAdmin: true
    adminEmail: "admin@jobflow.com"
    adminPassword: "admin"
    resetAdminPassword: true
